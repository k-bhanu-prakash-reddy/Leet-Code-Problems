class Solution {
    public int heightChecker(int[] heights) {
   int[] set = heights.clone();
    Arrays.sort(set);
    int sum=0;
    for(int i=0;i<heights.length;i++){
        if(heights[i]!=set[i]){
            sum++;
        }
    }
    return sum;
    }
}

