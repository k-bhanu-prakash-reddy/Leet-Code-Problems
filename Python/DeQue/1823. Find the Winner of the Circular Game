class Solution:
    def findTheWinner(self, n: int, k: int) -> int:    
        dq = deque(range(1,n+1))

        while len(dq) > 1 :

            for _ in range(k-1) :
                dq.append(dq.popleft())

            dq.popleft()
        return dq[0]        
