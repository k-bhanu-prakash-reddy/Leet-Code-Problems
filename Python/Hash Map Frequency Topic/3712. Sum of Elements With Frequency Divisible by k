class Solution:
    def sumDivisibleByK(self, nums: List[int], k: int) -> int:
        count = Counter(nums)
        summ = 0
        for key,value in count.items() :
            if value % k == 0 :
                summ += key * value
        return summ    
