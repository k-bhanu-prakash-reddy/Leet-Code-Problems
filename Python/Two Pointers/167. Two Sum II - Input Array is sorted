class Solution:
    def twoSum(self, numbers: List[int], target: int) -> List[int]:
        freq={}
        for i,num in enumerate(numbers) :
            complement = target-num
            if complement in freq :
                return [freq[complement],i+1]
            freq[num]=i+1 
