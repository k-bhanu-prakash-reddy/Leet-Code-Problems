class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        maximum = nums[0]    
        summ = 0
        for i in range(len(nums)) :
            summ += nums[i]
            if summ > maximum :
                maximum = summ
            if summ < 0 :
                summ = 0
        return maximum    
