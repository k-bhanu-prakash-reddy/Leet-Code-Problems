class Solution:
    def findPeakElement(self, nums: List[int]) -> int:
        maximum = float("-inf")
        summ =0
        for i in range(len(nums)) :
            if nums[i] > maximum :
                summ = i
            maximum = max(maximum,nums[i])    
        return summ
