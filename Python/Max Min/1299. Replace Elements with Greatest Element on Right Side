class Solution:
    def replaceElements(self, arr: List[int]) -> List[int]:
        
         maxright = -1
         for i in range(len(arr)-1,-1,-1) :
            current = arr[i]
            arr[i] = maxright 
            if current > maxright :
                maxright = current
         return arr       
